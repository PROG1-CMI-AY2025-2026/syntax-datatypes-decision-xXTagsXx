using System;

namespace MyConsoleApp

{
    public class Activity2
    {
        public string UmbrellaChecker(string weatherCondition)
        {
            // Create an umbrella checker program that will ask the user for the weather condition (choices: "rainy" or "sunny")
            // if 'rainy' return message should be 'Bring an umbrella!' 
            // if 'sunny' return message should be 'No umbrella needed.'
            // if response is not in the choices return 'Invalid weather condition.'

            return "Invalid weather condition.";
        }
    }
}